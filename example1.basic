PRINT("hello world!")

FUN add_wow(prefix) -> prefix + " wow"

FUN join(elements, separator)
    VAR result = ""
    VAR len = LEN(elements)

    FOR i = 0 TO len THEN
      VAR result = result + elements/i
      VAR result = result + separator

FOR i = 0 TO 5 THEN
  PRINT(add_wow("loop 5 times"))
END
